---
title: "Redaction"
description: "How Curia redacts sensitive data from LLM-facing content — built-in PII patterns, custom patterns, and where redaction is applied."
---

{/* TODO: This page needs to be written. Source from the curia codebase's redaction/PII filtering implementation. Cover: what's redacted by default (email, phone, CC, SSN), where redaction is applied (LLM-facing error messages, audit log entries), how to add custom PII patterns (cross-link to references/configuration for the pii.extra_patterns YAML), and the design principle of redacting at the boundary rather than at rest. */}

This page is under construction.
